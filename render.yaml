services:
  - type: web
    name: carecue-ai
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: GEMINI_MODEL
        value: gemini-1.5-flash
      - key: GOOGLE_API_KEY
        sync: false
